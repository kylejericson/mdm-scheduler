__pycache__/
*.py[cod]
.venv/
venv/
.env
*.db
*.db-wal
*.db-shm
data/
.pytest_cache/
.ruff_cache/
*.tar.gz
install.sh
.DS_Store

# Bootstrap is vendored into the image at build time, not committed.
# Only those two files - app JavaScript in static/ is source.
static/bootstrap.min.css
static/bootstrap.bundle.min.js
